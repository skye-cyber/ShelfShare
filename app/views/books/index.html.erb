<h1>Book Lending Library</h1>
<%= link_to "Add New Book", new_book_path, class: "btn btn-primary" %>
<table style="border: 1px solid black; margin-top: 20px; width: 100%;">
  <thead>
    <tr style="background-color: #f2f2f2;">
      <th style="text-align: left; padding: 10px;">Title</th>
      <th style="text-align: left; padding: 10px;">Author</th>
      <th style="text-align: left; padding: 10px;">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td style="padding: 10px;"><%= link_to book.title, book_path(book) %></td>
        <td style="padding: 10px;"><%= book.author %></td>
        <td style="padding: 10px;">
          <%= link_to "Edit", edit_book_path(book), class: "btn btn-success" %>
          <%= link_to "Delete", book_path(book), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
