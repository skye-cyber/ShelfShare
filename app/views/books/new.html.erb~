<div class="mt-4 max-w-lg md:max-w-4xl mx-auto p-8 bg-cyan-200 rounded-lg shadow-lg">
  <h1 class="text-3xl font-bold text-pink-600 mb-4">Add a New Book</h1>

  <%= form_with model: @book, local: true do |form| %>
    <% if @book.errors.any? %>
      <div id="error_explanation" class="bg-red-100 border border-red-500 text-red-900 px-4 py-3 rounded mb-4" role="alert">
        <h2 class="font-bold mb-2">
          <%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:
        </h2>
        <ul class="list-disc list-inside">
          <% @book.errors.full_messages.each do |message| %>
            <li class="text-red-700"><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-4">
      <%= form.label :title, class: "block text-gray-700 font-bold mb-2" %>
      <%= form.text_field :title, class: "w-full p-3 border border-gray-300 rounded" %>
    </div>

    <div class="mb-4">
      <%= form.label :author, class: "block text-gray-700 font-bold mb-2" %>
      <%= form.text_field :author, class: "w-full p-3 border border-gray-300 rounded" %>
    </div>

    <div class="mb-4">
      <%= form.label :description, class: "block text-gray-700 font-bold mb-2" %>
      <%= form.text_area :description, rows: 4, class: "w-full p-3 border border-gray-300 rounded" %>
    </div>

    <section class="flex justify-between">
        <div class="mt-4">
            <%= form.submit "Add Book", class: "px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition-colors" %>
        </div>
        <% end %>

        <p class="mt-6">
            <%= link_to "Back to List", books_path, class: "px-4 py-2 bg-sky-500 text-white rounded hover:bg-sky-600 transition-colors" %>
        </p>
    </section>
</div>
    <script>
    document.body.classList.add('bg-blue-100');
    </script>
